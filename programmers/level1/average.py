
def solution(arr):
    return (sum(i for i in arr) / len(arr))


print(solution([1, 2, 3, 4]))

# 다른 사람 풀이


def solution(arr):
    return (sum(list) / len(list))

# 만약 문제에 1이상 배열이 없으면 분모가 0인 경우도 생각해야함


def solution(arr):
    if len(list) == 0:
        return 0
